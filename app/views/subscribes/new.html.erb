<div class="container-sm shadow-lg">
    <%= form_with(model: @subscribe) do |form| %>
    <% if @subscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscribe.errors.count, "error") %> prohibited this subscribe from being saved:</h2>

      <ul>
        <% @subscribe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <br/>
  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, value: current_user.id %>
  </div>

  <div class="field">
    <%= form.label :plan_id %>
    <%= form.text_field :plan_id, value: @subscribe.plan_id %>
  </div>
  <br>

  <button type="submit" class="btn btn-primary">Create Subscribe</button>
  <% end %>
</div>
